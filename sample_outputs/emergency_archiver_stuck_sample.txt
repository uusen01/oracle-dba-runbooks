-- EMERGENCY: ORA-00257, new connections failing on ORADEMO (fictional incident).

-- Symptom (from a connection attempt / alert log):
ORA-00257: Archiver error. Connect AS SYSDBA only until resolved.

-- Diagnose: FRA full?
SQL> SELECT name, ROUND(space_used*100/NULLIF(space_limit,0),1) AS pct_used
  2    FROM v$recovery_file_dest;
NAME                                      PCT_USED
---------------------------------------- --------
/u01/app/oracle/fast_recovery_area          100.0     <-- FRA full, archiver stalled

SQL> SELECT file_type, percent_space_used, percent_space_reclaimable
  2    FROM v$flash_recovery_area_usage WHERE number_of_files > 0;
FILE_TYPE                PERCENT_SPACE_USED PERCENT_SPACE_RECLAIMABLE
------------------------ ------------------ -------------------------
ARCHIVED LOG                           78.0                       0.0   <-- nothing backed up

-- Root: nightly archivelog backup had FAILED silently for 2 nights, so nothing was
-- reclaimable. Fix = back up archivelogs, then reclaim:
RMAN> BACKUP ARCHIVELOG ALL;
RMAN> DELETE ARCHIVELOG ALL BACKED UP 1 TIMES TO DISK COMPLETED BEFORE 'SYSDATE-1';
...deleted; FRA now 41% used.

-- Service restored: archiver resumed, new connections succeed.
SQL> ARCHIVE LOG LIST;
Database log mode              Archive Mode
Automatic archival             Enabled
-- (no longer stuck)

-- Read: textbook ORA-00257 -- the FRA filled because the archive-log backup had been
-- failing unnoticed, so no logs were reclaimable. Recovery was NOT deleting files at the
-- OS level (that breaks recoverability) but backing them up via RMAN and then deleting
-- the backed-up copies. Total downtime ~33 min. Follow-ups (see the Post-Incident Review
-- Template): alert on job failures and on FRA %-used, and revisit FRA sizing.
